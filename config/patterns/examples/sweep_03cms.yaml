# Provisional example pattern. Placeholder channel IDs and arbitrary overlap.
# Real patterns come from the hardware bring-up session; see SPEC.md 12.2 and 20.
name: sweep_03cms
row_interval_ms: 500.0
channel_ids: [1, 2, 3, 4, 5]
nominal_velocity_cm_s: 3.0
assumed_channel_spacing_cm: 1.5
overlap_rows: 1
loop: true
# Derived, for reference only - the CSV and row_interval_ms are authoritative:
#   leading-edge onset span: 2.0 s over 6.0 cm = 3.0 cm/s
#   one full sweep including the tail: 3.0 s
