# Every screen state the software can show, with what the image should contain.
# SPEC.md 17.4. Generated by `make shots ARGS=--write-manifest`; the descriptions
# come from the catalogue in tatp/screenshots.py, so edit them there, not here.
#
# An entry with no image is a failure, and an image with no entry is a failure. What
# is compared against screenshots/reference/ is only what has been approved.

screens:
  experimenter_en_abort_dialog: 'The abort confirmation: what happens to the data,
    a reason field, and Abort disabled until a reason is typed. (en)'
  experimenter_en_all_banners: 'All three banners at once: nothing below the reserved
    region has moved (UI_PRINCIPLES.md 3.3). The fit-preview banner is amber like
    the reduced-capability one (SPEC.md 11.1). (en)'
  experimenter_en_awaiting_rebalance: 'The evenness question answered ''uneven'':
    Rebalance and Start block both enabled, because the procedure waits on either.
    (en)'
  experimenter_en_block_due: 'The block is due: amber and bold in the same place as
    the countdown. (en)'
  experimenter_en_block_overdue: 'The block is overdue: red and bold, OVERDUE in capitals.
    (en)'
  experimenter_en_block_pinprick: 'A pinprick block in the intervention: the countdown
    to the next block in grey, no pressures (hidden for blinding, SPEC.md 16) and
    no placeholder in their place. (en)'
  experimenter_en_block_touch: 'A touch block in the intervention: the one line shown
    at every touch start in every condition, and nothing else that could differ between
    them. (en)'
  experimenter_en_brush_secondary: 'A brush stroke in the secondary zone: the dashed
    ellipse outlined. (en)'
  experimenter_en_countdown: 'Waiting for the next block: ''Next: block 3 (touch)
    in 04:05'', grey. (en)'
  experimenter_en_designer_grid: 'An example pattern opened on the Grid tab: one row
    per row interval with its time, one column per channel id, on cells filled. The
    timeline below draws two cycles because it loops, the repeat fainter, and the
    line under it says it loads. (en)'
  experimenter_en_designer_new: 'A new pattern: nothing filled in and nothing assumed.
    The line in red says why it does not load yet, and Save, Export and Play are disabled.
    (en)'
  experimenter_en_designer_off_grid: A timing off the row grid, refused in red with
    the reason and never rounded. The grid converted before it is kept, unchanged,
    and still previewed. (en)
  experimenter_en_designer_ordered: 'Ordered channels: five channels with their hold
    times, the join-and-hold mode and the delay, converted. The message says the grid
    was replaced, and the timeline shows the overlapping sweep. (en)'
  experimenter_en_designer_playing: 'Playback on the mock garment: the cursor in amber
    on the timeline and the channels on now named, their labels amber. Set directly
    rather than played, because a running pattern would never be photographed at the
    same moment twice. (en)'
  experimenter_en_designer_timed: 'Timed channels: onset and offset per channel in
    ms, converted. Played once, so the timeline draws one cycle. (en)'
  experimenter_en_disconnected: 'The garment reported disconnected: red and bold in
    the hardware panel, Connect enabled and Disconnect not. (en)'
  experimenter_en_distances_dialog: 'The mapping distances window (SPEC.md 8.4): the
    latest mapped phase selected, one field per path in path order, two typed with
    either decimal mark. Non-modal: the session runs on behind it. (en)'
  experimenter_en_error_dialog: 'The error dialog: a red title and the message as
    raised. (en)'
  experimenter_en_fit_preview_f40: 'The main window while the F40 fit preview (SPEC.md
    11.1) is open, only with fit_preview.enabled: the banner saying what it costs,
    Accept and Re-run enabled, and no rating anywhere on this window -- they are in
    the preview window. (en)'
  experimenter_en_fit_preview_f40_window: 'The fit preview window itself for `fit_preview_f40`:
    the plot, the quality, what the session will use, and Accept / Re-run (en).'
  experimenter_en_fit_preview_touch: 'The main window while the touch-calibration
    fit preview is open: its instruction at a smaller size because it is long, Accept
    and Re-run enabled. (en)'
  experimenter_en_fit_preview_touch_window: 'The fit preview window itself for `fit_preview_touch`:
    the plot, the quality, what the session will use, and Accept / Re-run (en).'
  experimenter_en_hardware_pressures: 'Touch calibration: the hardware panel with
    every channel''s pressure and one fault, in red. (en)'
  experimenter_en_interrupted_emergency_stop: 'The participant pressed the stop: the
    red line under the phase, Resume and Abort the only controls enabled, and nothing
    else moved. (en)'
  experimenter_en_interrupted_pause: 'The experimenter''s pause: the same line in
    amber, Resume enabled. (en)'
  experimenter_en_intervention_fault: 'A garment fault during the intervention: shown
    in red, counted, and without the channel, which could say which pattern is running
    (SPEC.md 16). (en)'
  experimenter_en_launcher: The launcher's four entries (SPEC.md 4.1), all enabled.
    Design a pattern says it is for S only, because it shows pattern names and shapes
    (en).
  experimenter_en_launcher_instruments: 'Instruments and environment (SPEC.md 8.1):
    every filament with its label, size, nominal force and a measured-force field;
    the weighing date and balance; the optional room temperature and humidity (en).'
  experimenter_en_launcher_preview: 'The schedule preview (SPEC.md 7.2): tools/preview_schedule.py''s
    report, read-only (en).'
  experimenter_en_launcher_resume: 'The resume question (SPEC.md 15): what was completed
    and how long ago sensitisation began. Resume, or an explicit new session; closing
    it starts nothing (en).'
  experimenter_en_launcher_session: 'The session dialog as it opens: the data folder
    filled in from config; the session number, both languages and the pattern folder
    unchosen, with no default; Start disabled until a check passes (en).'
  experimenter_en_launcher_session_checked: 'The session dialog after Check: a preflight
    warning listed in amber, Start enabled because nothing refuses (en).'
  experimenter_en_long_instruction: 'The longest instruction the session gives, set
    down the type scale so it fits its region: nothing below it has moved, and the
    controls are where they always are. (en)'
  experimenter_en_mapping: 'A mapping path: the secondary zone outlined without a
    site, the technique text shown because a filament is used, and Mapping distances
    enabled now that a phase has been mapped. (en)'
  experimenter_en_open_items: Unresolved open items listed for the experimenter (SPEC.md
    20). (en)
  experimenter_en_pinprick_primary: 'A pinprick application: the filament by its gram
    label, the primary zone outlined on the diagram with its site beside it, the technique
    text below, and the response pending -- never its value. (en)'
  experimenter_en_placeholder_banner: The unapproved-wording banner (SPEC.md 12.4),
    red and unmissable. (en)
  experimenter_en_plain: No banners, no open items, nothing scheduled -- a correctly
    configured session at setup. Controls along the bottom, Resume, Rebalance and
    the fit choice disabled; the zone diagram with no zone marked; the hardware panel
    connected with no pressures. (en)
  experimenter_en_reduced_capability_banner: The reduced-capability banner naming
    the driver in use (SPEC.md 12.4). (en)
  experimenter_en_rekindle_due: 'The rekindle is due: a timed event that is not a
    block, named by its phase. (en)'
  experimenter_en_rerun_dialog: The re-run reason, asked before a fit is discarded
    (SPEC.md 11.1). (en)
  experimenter_sv_abort_dialog: 'The abort confirmation: what happens to the data,
    a reason field, and Abort disabled until a reason is typed. (sv)'
  experimenter_sv_all_banners: 'All three banners at once: nothing below the reserved
    region has moved (UI_PRINCIPLES.md 3.3). The fit-preview banner is amber like
    the reduced-capability one (SPEC.md 11.1). (sv)'
  experimenter_sv_awaiting_rebalance: 'The evenness question answered ''uneven'':
    Rebalance and Start block both enabled, because the procedure waits on either.
    (sv)'
  experimenter_sv_block_due: 'The block is due: amber and bold in the same place as
    the countdown. (sv)'
  experimenter_sv_block_overdue: 'The block is overdue: red and bold, OVERDUE in capitals.
    (sv)'
  experimenter_sv_block_pinprick: 'A pinprick block in the intervention: the countdown
    to the next block in grey, no pressures (hidden for blinding, SPEC.md 16) and
    no placeholder in their place. (sv)'
  experimenter_sv_block_touch: 'A touch block in the intervention: the one line shown
    at every touch start in every condition, and nothing else that could differ between
    them. (sv)'
  experimenter_sv_brush_secondary: 'A brush stroke in the secondary zone: the dashed
    ellipse outlined. (sv)'
  experimenter_sv_countdown: 'Waiting for the next block: ''Next: block 3 (touch)
    in 04:05'', grey. (sv)'
  experimenter_sv_designer_grid: 'An example pattern opened on the Grid tab: one row
    per row interval with its time, one column per channel id, on cells filled. The
    timeline below draws two cycles because it loops, the repeat fainter, and the
    line under it says it loads. (sv)'
  experimenter_sv_designer_new: 'A new pattern: nothing filled in and nothing assumed.
    The line in red says why it does not load yet, and Save, Export and Play are disabled.
    (sv)'
  experimenter_sv_designer_off_grid: A timing off the row grid, refused in red with
    the reason and never rounded. The grid converted before it is kept, unchanged,
    and still previewed. (sv)
  experimenter_sv_designer_ordered: 'Ordered channels: five channels with their hold
    times, the join-and-hold mode and the delay, converted. The message says the grid
    was replaced, and the timeline shows the overlapping sweep. (sv)'
  experimenter_sv_designer_playing: 'Playback on the mock garment: the cursor in amber
    on the timeline and the channels on now named, their labels amber. Set directly
    rather than played, because a running pattern would never be photographed at the
    same moment twice. (sv)'
  experimenter_sv_designer_timed: 'Timed channels: onset and offset per channel in
    ms, converted. Played once, so the timeline draws one cycle. (sv)'
  experimenter_sv_disconnected: 'The garment reported disconnected: red and bold in
    the hardware panel, Connect enabled and Disconnect not. (sv)'
  experimenter_sv_distances_dialog: 'The mapping distances window (SPEC.md 8.4): the
    latest mapped phase selected, one field per path in path order, two typed with
    either decimal mark. Non-modal: the session runs on behind it. (sv)'
  experimenter_sv_error_dialog: 'The error dialog: a red title and the message as
    raised. (sv)'
  experimenter_sv_fit_preview_f40: 'The main window while the F40 fit preview (SPEC.md
    11.1) is open, only with fit_preview.enabled: the banner saying what it costs,
    Accept and Re-run enabled, and no rating anywhere on this window -- they are in
    the preview window. (sv)'
  experimenter_sv_fit_preview_f40_window: 'The fit preview window itself for `fit_preview_f40`:
    the plot, the quality, what the session will use, and Accept / Re-run (sv).'
  experimenter_sv_fit_preview_touch: 'The main window while the touch-calibration
    fit preview is open: its instruction at a smaller size because it is long, Accept
    and Re-run enabled. (sv)'
  experimenter_sv_fit_preview_touch_window: 'The fit preview window itself for `fit_preview_touch`:
    the plot, the quality, what the session will use, and Accept / Re-run (sv).'
  experimenter_sv_hardware_pressures: 'Touch calibration: the hardware panel with
    every channel''s pressure and one fault, in red. (sv)'
  experimenter_sv_interrupted_emergency_stop: 'The participant pressed the stop: the
    red line under the phase, Resume and Abort the only controls enabled, and nothing
    else moved. (sv)'
  experimenter_sv_interrupted_pause: 'The experimenter''s pause: the same line in
    amber, Resume enabled. (sv)'
  experimenter_sv_intervention_fault: 'A garment fault during the intervention: shown
    in red, counted, and without the channel, which could say which pattern is running
    (SPEC.md 16). (sv)'
  experimenter_sv_launcher: The launcher's four entries (SPEC.md 4.1), all enabled.
    Design a pattern says it is for S only, because it shows pattern names and shapes
    (sv).
  experimenter_sv_launcher_instruments: 'Instruments and environment (SPEC.md 8.1):
    every filament with its label, size, nominal force and a measured-force field;
    the weighing date and balance; the optional room temperature and humidity (sv).'
  experimenter_sv_launcher_preview: 'The schedule preview (SPEC.md 7.2): tools/preview_schedule.py''s
    report, read-only (sv).'
  experimenter_sv_launcher_resume: 'The resume question (SPEC.md 15): what was completed
    and how long ago sensitisation began. Resume, or an explicit new session; closing
    it starts nothing (sv).'
  experimenter_sv_launcher_session: 'The session dialog as it opens: the data folder
    filled in from config; the session number, both languages and the pattern folder
    unchosen, with no default; Start disabled until a check passes (sv).'
  experimenter_sv_launcher_session_checked: 'The session dialog after Check: a preflight
    warning listed in amber, Start enabled because nothing refuses (sv).'
  experimenter_sv_long_instruction: 'The longest instruction the session gives, set
    down the type scale so it fits its region: nothing below it has moved, and the
    controls are where they always are. (sv)'
  experimenter_sv_mapping: 'A mapping path: the secondary zone outlined without a
    site, the technique text shown because a filament is used, and Mapping distances
    enabled now that a phase has been mapped. (sv)'
  experimenter_sv_open_items: Unresolved open items listed for the experimenter (SPEC.md
    20). (sv)
  experimenter_sv_pinprick_primary: 'A pinprick application: the filament by its gram
    label, the primary zone outlined on the diagram with its site beside it, the technique
    text below, and the response pending -- never its value. (sv)'
  experimenter_sv_placeholder_banner: The unapproved-wording banner (SPEC.md 12.4),
    red and unmissable. (sv)
  experimenter_sv_plain: No banners, no open items, nothing scheduled -- a correctly
    configured session at setup. Controls along the bottom, Resume, Rebalance and
    the fit choice disabled; the zone diagram with no zone marked; the hardware panel
    connected with no pressures. (sv)
  experimenter_sv_reduced_capability_banner: The reduced-capability banner naming
    the driver in use (SPEC.md 12.4). (sv)
  experimenter_sv_rekindle_due: 'The rekindle is due: a timed event that is not a
    block, named by its phase. (sv)'
  experimenter_sv_rerun_dialog: The re-run reason, asked before a fit is discarded
    (SPEC.md 11.1). (sv)
  participant_en_adjust_just_noticeable: 'The adjustment screen for `just_noticeable`:
    the target, both large buttons drawn with their labels, and the confirm sentence,
    en.'
  participant_en_adjust_just_uncomfortable: 'The adjustment screen for `just_uncomfortable`:
    the target, both large buttons drawn with their labels, and the confirm sentence,
    en.'
  participant_en_adjust_match: 'The adjustment screen for `match`: the target, both
    large buttons drawn with their labels, and the confirm sentence, en.'
  participant_en_adjust_most_pleasant: 'The adjustment screen for `most_pleasant`:
    the target, both large buttons drawn with their labels, and the confirm sentence,
    en.'
  participant_en_audio_setup_find_level: 'The masking check''s `audio_setup.find_level`
    (SPEC.md 10.7), a text screen: its wording names the buttons itself, so none are
    drawn, en.'
  participant_en_audio_setup_mask_level: 'The masking check''s `audio_setup.mask_level`
    (SPEC.md 10.7), a text screen: its wording names the buttons itself, so none are
    drawn, en.'
  participant_en_audio_setup_settled: 'The masking check''s `audio_setup.settled`
    (SPEC.md 10.7), a text screen: its wording names the buttons itself, so none are
    drawn, en.'
  participant_en_blank: Nothing at all -- what the participant sees during a stimulus.
  participant_en_choice_comparison_accepting: 'Both stimuli delivered, no emphasis,
    the next press is the answer. Identical to the waiting state by design: nothing
    on screen may hint that a press is due.'
  participant_en_choice_comparison_chosen_right: The right button shown back as chosen
    -- filled, its symbol reversed out. Set directly rather than by pressing, because
    the press starts a timer that would blank the screen before it could be grabbed.
  participant_en_choice_comparison_emphasis_left: The left button's outline thickened
    while its stimulus plays (UI_PRINCIPLES.md 5.11). It must read as `this is the
    one you are feeling`, never as a suggestion.
  participant_en_choice_comparison_waiting: '`choices.comparison` with both buttons
    drawn and neither emphasised -- the state before the first stimulus, en.'
  participant_en_choice_evenness_accepting: 'Both stimuli delivered, no emphasis,
    the next press is the answer. Identical to the waiting state by design: nothing
    on screen may hint that a press is due.'
  participant_en_choice_evenness_chosen_right: The right button shown back as chosen
    -- filled, its symbol reversed out. Set directly rather than by pressing, because
    the press starts a timer that would blank the screen before it could be grabbed.
  participant_en_choice_evenness_waiting: '`choices.evenness` with both buttons drawn
    and neither emphasised -- the state before the first stimulus, en.'
  participant_en_choice_still_audible_accepting: 'Both stimuli delivered, no emphasis,
    the next press is the answer. Identical to the waiting state by design: nothing
    on screen may hint that a press is due.'
  participant_en_choice_still_audible_chosen_right: The right button shown back as
    chosen -- filled, its symbol reversed out. Set directly rather than by pressing,
    because the press starts a timer that would blank the screen before it could be
    grabbed.
  participant_en_choice_still_audible_waiting: '`choices.still_audible` with both
    buttons drawn and neither emphasised -- the state before the first stimulus, en.'
  participant_en_cue: 'The visual warning cue: one filled disc, centred, no wording.'
  participant_en_preference: 'The preference selection (SPEC.md 9 step 6): the opening
    line, both large buttons drawn with their labels, and the confirm sentence, en.'
  participant_en_screen_emergency_stop: The participant text `screens.emergency_stop`,
    centred, en.
  participant_en_screen_paused: The participant text `screens.paused`, centred, en.
  participant_en_screen_self_start: The participant text `screens.self_start`, centred,
    en.
  participant_en_screen_session_end: The participant text `screens.session_end`, centred,
    en.
  participant_en_screen_standby: The participant text `screens.standby`, centred,
    en.
  participant_en_screen_stop_rehearsal: The participant text `screens.stop_rehearsal`,
    centred, en.
  participant_en_screen_stop_rehearsal_done: The participant text `screens.stop_rehearsal_done`,
    centred, en.
  participant_en_screen_welcome: The participant text `screens.welcome`, centred,
    en.
  participant_en_vas_alertness_uncued: The `alertness` scale with its question and
    anchors and NO marker -- the state before the first press, en.
  participant_en_vas_intensity_uncued: The `intensity` scale with its question and
    anchors and NO marker -- the state before the first press, en.
  participant_en_vas_pain_at_50pct: The marker at 50 % along the line, no numbers
    and no tick marks beyond the labelled anchors.
  participant_en_vas_pain_at_5pct: The marker at 5 % along the line, no numbers and
    no tick marks beyond the labelled anchors.
  participant_en_vas_pain_at_95pct: The marker at 95 % along the line, no numbers
    and no tick marks beyond the labelled anchors.
  participant_en_vas_pain_first_press_left: The marker where a first left press puts
    it -- SPEC.md 10.2's rule that the marker appears on the side that was pressed.
  participant_en_vas_pain_first_press_right: The marker where a first right press
    puts it -- SPEC.md 10.2's rule that the marker appears on the side that was pressed.
  participant_en_vas_pain_uncued: The `pain` scale with its question and anchors and
    NO marker -- the state before the first press, en.
  participant_en_vas_pleasantness_uncued: The `pleasantness` scale with its question
    and anchors and NO marker -- the state before the first press, en.
  participant_en_vas_relaxation_uncued: The `relaxation` scale with its question and
    anchors and NO marker -- the state before the first press, en.
  participant_sv_adjust_just_noticeable: 'The adjustment screen for `just_noticeable`:
    the target, both large buttons drawn with their labels, and the confirm sentence,
    sv.'
  participant_sv_adjust_just_uncomfortable: 'The adjustment screen for `just_uncomfortable`:
    the target, both large buttons drawn with their labels, and the confirm sentence,
    sv.'
  participant_sv_adjust_match: 'The adjustment screen for `match`: the target, both
    large buttons drawn with their labels, and the confirm sentence, sv.'
  participant_sv_adjust_most_pleasant: 'The adjustment screen for `most_pleasant`:
    the target, both large buttons drawn with their labels, and the confirm sentence,
    sv.'
  participant_sv_audio_setup_find_level: 'The masking check''s `audio_setup.find_level`
    (SPEC.md 10.7), a text screen: its wording names the buttons itself, so none are
    drawn, sv.'
  participant_sv_audio_setup_mask_level: 'The masking check''s `audio_setup.mask_level`
    (SPEC.md 10.7), a text screen: its wording names the buttons itself, so none are
    drawn, sv.'
  participant_sv_audio_setup_settled: 'The masking check''s `audio_setup.settled`
    (SPEC.md 10.7), a text screen: its wording names the buttons itself, so none are
    drawn, sv.'
  participant_sv_blank: Nothing at all -- what the participant sees during a stimulus.
  participant_sv_choice_comparison_accepting: 'Both stimuli delivered, no emphasis,
    the next press is the answer. Identical to the waiting state by design: nothing
    on screen may hint that a press is due.'
  participant_sv_choice_comparison_chosen_right: The right button shown back as chosen
    -- filled, its symbol reversed out. Set directly rather than by pressing, because
    the press starts a timer that would blank the screen before it could be grabbed.
  participant_sv_choice_comparison_emphasis_left: The left button's outline thickened
    while its stimulus plays (UI_PRINCIPLES.md 5.11). It must read as `this is the
    one you are feeling`, never as a suggestion.
  participant_sv_choice_comparison_waiting: '`choices.comparison` with both buttons
    drawn and neither emphasised -- the state before the first stimulus, sv.'
  participant_sv_choice_evenness_accepting: 'Both stimuli delivered, no emphasis,
    the next press is the answer. Identical to the waiting state by design: nothing
    on screen may hint that a press is due.'
  participant_sv_choice_evenness_chosen_right: The right button shown back as chosen
    -- filled, its symbol reversed out. Set directly rather than by pressing, because
    the press starts a timer that would blank the screen before it could be grabbed.
  participant_sv_choice_evenness_waiting: '`choices.evenness` with both buttons drawn
    and neither emphasised -- the state before the first stimulus, sv.'
  participant_sv_choice_still_audible_accepting: 'Both stimuli delivered, no emphasis,
    the next press is the answer. Identical to the waiting state by design: nothing
    on screen may hint that a press is due.'
  participant_sv_choice_still_audible_chosen_right: The right button shown back as
    chosen -- filled, its symbol reversed out. Set directly rather than by pressing,
    because the press starts a timer that would blank the screen before it could be
    grabbed.
  participant_sv_choice_still_audible_waiting: '`choices.still_audible` with both
    buttons drawn and neither emphasised -- the state before the first stimulus, sv.'
  participant_sv_cue: 'The visual warning cue: one filled disc, centred, no wording.'
  participant_sv_preference: 'The preference selection (SPEC.md 9 step 6): the opening
    line, both large buttons drawn with their labels, and the confirm sentence, sv.'
  participant_sv_screen_emergency_stop: The participant text `screens.emergency_stop`,
    centred, sv.
  participant_sv_screen_paused: The participant text `screens.paused`, centred, sv.
  participant_sv_screen_self_start: The participant text `screens.self_start`, centred,
    sv.
  participant_sv_screen_session_end: The participant text `screens.session_end`, centred,
    sv.
  participant_sv_screen_standby: The participant text `screens.standby`, centred,
    sv.
  participant_sv_screen_stop_rehearsal: The participant text `screens.stop_rehearsal`,
    centred, sv.
  participant_sv_screen_stop_rehearsal_done: The participant text `screens.stop_rehearsal_done`,
    centred, sv.
  participant_sv_screen_welcome: The participant text `screens.welcome`, centred,
    sv.
  participant_sv_vas_alertness_uncued: The `alertness` scale with its question and
    anchors and NO marker -- the state before the first press, sv.
  participant_sv_vas_intensity_uncued: The `intensity` scale with its question and
    anchors and NO marker -- the state before the first press, sv.
  participant_sv_vas_pain_at_50pct: The marker at 50 % along the line, no numbers
    and no tick marks beyond the labelled anchors.
  participant_sv_vas_pain_at_5pct: The marker at 5 % along the line, no numbers and
    no tick marks beyond the labelled anchors.
  participant_sv_vas_pain_at_95pct: The marker at 95 % along the line, no numbers
    and no tick marks beyond the labelled anchors.
  participant_sv_vas_pain_first_press_left: The marker where a first left press puts
    it -- SPEC.md 10.2's rule that the marker appears on the side that was pressed.
  participant_sv_vas_pain_first_press_right: The marker where a first right press
    puts it -- SPEC.md 10.2's rule that the marker appears on the side that was pressed.
  participant_sv_vas_pain_uncued: The `pain` scale with its question and anchors and
    NO marker -- the state before the first press, sv.
  participant_sv_vas_pleasantness_uncued: The `pleasantness` scale with its question
    and anchors and NO marker -- the state before the first press, sv.
  participant_sv_vas_relaxation_uncued: The `relaxation` scale with its question and
    anchors and NO marker -- the state before the first press, sv.
